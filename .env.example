# Kopiere diese Datei nach .env und trage echte Werte ein.
# .env wird niemals ins Git-Repo eingecheckt.

# Produktiv-DB (data-User, Schreibzugriff auf base.* und mart.*)
TSU_PROD_POSTGRES_URL=postgresql://data:CHANGEME@localhost:5432/tsu

# Test-DB (lokal, für Entwicklung)
TSU_TEST_POSTGRES_URL=postgresql://tsu_dev:CHANGEME@localhost:5432/tsu_test

# Alter racing-Server (read-only, für migrate_elo_history.py)
OLD_RACING_POSTGRES_URL=postgresql://racing:CHANGEME@185.170.113.38/racing
